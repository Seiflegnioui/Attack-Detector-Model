\documentclass[12pt,a4paper]{report}

% ═══════════════════════════════════════════
% PACKAGES
% ═══════════════════════════════════════════
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{lmodern}
\usepackage{amsmath, amsfonts, amssymb}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{listings}
\usepackage{setspace}
\usepackage{parskip}

% ═══════════════════════════════════════════
% CONFIGURATION
% ═══════════════════════════════════════════
\geometry{top=2.5cm, bottom=2.5cm, left=3cm, right=2.5cm}
\setstretch{1.25}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!70!black,
    citecolor=green!50!black,
    urlcolor=cyan!70!black,
    pdftitle={Rapport PFM — IDS par Machine Learning},
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\nouppercase{\leftmark}}
\fancyhead[R]{\small Projet de Fin de Module}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% Style des listings code
\lstset{
    basicstyle=\small\ttfamily,
    breaklines=true,
    frame=single,
    backgroundcolor=\color{gray!5},
    rulecolor=\color{gray!30},
    keywordstyle=\color{blue!70!black},
    commentstyle=\color{green!50!black},
    stringstyle=\color{red!60!black},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=8pt,
    tabsize=4,
    showstringspaces=false,
}

\begin{document}

% ═══════════════════════════════════════════
% PAGE DE GARDE
% ═══════════════════════════════════════════
\begin{titlepage}
    \centering
    \vspace*{1cm}

    % ── Logo université (décommenter et remplacer le chemin si disponible) ──
    % \includegraphics[width=4cm]{logo_universite.png}\\[1cm]

    {\large\textsc{Université Hassan II de Casablanca}\\[0.3cm]}
    {\large\textsc{Faculté des Sciences Ben M'Sick}\\[0.3cm]}
    {\large\textsc{Master d'Excellence en Intelligence Artificielle}\\[0.3cm]}

    \vspace{0.8cm}

    \rule{\textwidth}{0.5pt}
    \vspace{0.8cm}
    {\LARGE\bfseries Conception et Implémentation d'un\\Système de Détection d'Intrusions Réseau\\Basé sur le Machine Learning\par}
    \vspace{0.5cm}
    {\large\itshape Application MLOps sur le Dataset NSL-KDD\par}
    \vspace{0.3cm}
    \rule{\textwidth}{0.5pt}

    \vspace{2cm}

    {\large\textbf{Rapport de Projet de Fin de Module}\par}

    \vspace{1.5cm}

    \begin{tabular}{rl}
        \textbf{Réalisé par :} & Youssef Sarraf \\[0.15cm]
        & Sifeddine Legnioui \\[0.3cm]
        \textbf{Encadré par :} & Pr. Oumaima GUENDOUL \\[0.3cm]
        \textbf{Module :} & Machine Learning \\
    \end{tabular}

    \vfill

    {\large Année universitaire 2025--2026\par}
\end{titlepage}

% ═══════════════════════════════════════════
% PAGES PRÉLIMINAIRES
% ═══════════════════════════════════════════
\pagenumbering{roman}

% ── Remerciements ──
\chapter*{Remerciements}
\addcontentsline{toc}{chapter}{Remerciements}

Nous tenons à remercier sincèrement \textbf{Pr. Oumaima GUENDOUL}, responsable du module de Machine Learning, pour la qualité de son enseignement et ses orientations tout au long du semestre. Les compétences acquises en ingénierie des données, en modélisation et en mise en production (MLOps) ont été essentielles pour mener à bien ce projet.

Nous remercions également l'ensemble de l'équipe pédagogique de la Faculté des Sciences Ben M'Sick pour l'environnement stimulant qu'ils offrent, favorisant l'apprentissage par la pratique et l'autonomie technique.

\vspace{2cm}

% ── Résumé ──
\chapter*{Résumé}
\addcontentsline{toc}{chapter}{Résumé}

La détection d'intrusions réseau constitue un enjeu majeur de la cybersécurité moderne. Les systèmes classiques, fondés sur des bases de signatures statiques, peinent à détecter les attaques inédites (\textit{Zero-Day}) et les comportements anormaux subtils. Ce projet propose une approche d'ingénierie appliquée, basée sur l'apprentissage automatique (\textit{Machine Learning}), pour la classification du trafic réseau en temps réel.

En utilisant le jeu de données de référence NSL-KDD, composé de 125\,973 connexions d'entraînement, nous avons développé un pipeline analytique complet. Le projet se concentre particulièrement sur les défis liés à la mise en production (MLOps) et à la gestion du déséquilibre extrême des classes. L'utilisation de la technique SMOTE (\textit{Synthetic Minority Over-sampling Technique}), intégrée de manière stricte dans la boucle de validation croisée pour éviter toute fuite de données, a permis de pallier la rareté critique de certaines attaques.

Les algorithmes ensemblistes Random Forest et XGBoost ont été retenus pour leur adéquation avec les données tabulaires hautement dimensionnelles. Les résultats montrent que XGBoost atteint une exactitude de 78,95\% et un F1-Score Macro de 0,631, avec des performances remarquables sur les classes minoritaires critiques : une précision de 97,11\% pour les attaques R2L et un F1-Score triplé (0,46) pour les attaques U2R.

L'ensemble a été industrialisé sous forme d'une API REST asynchrone (FastAPI) minimisant la latence d'inférence, accompagnée d'un tableau de bord interactif, le tout conteneurisé via Docker pour un déploiement reproductible.

\textbf{Mots-clés :} Détection d'intrusions, Machine Learning Engineering, XGBoost, SMOTE, NSL-KDD, Déséquilibre de classes, MLOps, FastAPI, Docker.

\vspace{1cm}

% ── Abstract ──
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

Network intrusion detection is a critical challenge in modern cybersecurity. Traditional signature-based systems fail to detect unknown (\textit{Zero-Day}) attacks and subtle anomalous behaviors. This project proposes an applied engineering approach, based on Machine Learning, for real-time network traffic classification.

Using the NSL-KDD benchmark dataset (125,973 training connections), we developed a complete analytical pipeline. The project specifically focuses on production deployment challenges (MLOps) and handling extreme class imbalance. The use of SMOTE (\textit{Synthetic Minority Over-sampling Technique}), strictly integrated within the cross-validation loop to prevent data leakage, addressed the critical scarcity of certain attacks.

Ensemble algorithms Random Forest and XGBoost were selected for their adequacy with highly dimensional tabular data. Results show that XGBoost achieves 78.95\% accuracy and a Macro F1-Score of 0.631, with remarkable performance on critical minority classes: 97.11\% precision for R2L attacks and a tripled F1-Score (0.46) for U2R attacks.

The system was industrialized as an asynchronous REST API (FastAPI) minimizing inference latency, accompanied by an interactive dashboard, all containerized via Docker for reproducible deployment.

\textbf{Keywords:} Intrusion Detection, Machine Learning Engineering, XGBoost, SMOTE, NSL-KDD, Class Imbalance, MLOps, FastAPI, Docker.

% ═══════════════════════════════════════════
% TABLE DES MATIÈRES
% ═══════════════════════════════════════════
\tableofcontents
\listoffigures
\listoftables

\newpage
\pagenumbering{arabic}

% ═══════════════════════════════════════════
% CHAPITRE 1 — INTRODUCTION
% ═══════════════════════════════════════════
\chapter{Introduction Générale}

\section{Contexte de la sécurité réseau}

L'ère numérique a transformé radicalement les infrastructures informatiques mondiales. Les réseaux d'entreprise sont devenus à la fois plus complexes et plus exposés aux menaces. Les cyberattaques modernes ne se limitent plus aux virus informatiques traditionnels, elles englobent un spectre sophistiqué : dénis de service (DoS), intrusions furtives (\textit{Remote-to-Local}), ou encore des élévations de privilèges (\textit{User-to-Root}) permettant à un attaquant d'obtenir un contrôle total.

Face à cette menace, les Systèmes de Détection d'Intrusions (IDS) classiques se basent sur des bibliothèques de signatures (comme Snort ou Suricata). Si cette approche est fiable contre les menaces documentées, elle reste aveugle face aux attaques \textit{Zero-Day}. Le \textit{Machine Learning} offre un paradigme comportemental puissant : identifier le trafic malveillant en apprenant les caractéristiques discriminantes directement à partir de grandes quantités de données historiques.

\section{Problématique d'ingénierie}

Ce projet ne se contente pas d'entraîner un modèle théorique, il vise à résoudre une problématique d'ingénierie globale :
\begin{quote}
    \textit{Comment concevoir, évaluer et déployer un pipeline ML robuste de détection d'intrusions capable de traiter en temps réel un trafic réseau déséquilibré, tout en évitant les biais méthodologiques inhérents aux environnements académiques ?}
\end{quote}

Cette problématique soulève plusieurs verrous technologiques :
\begin{enumerate}
    \item \textbf{Le déséquilibre extrême} : Comment détecter une attaque U2R qui représente moins de 0,04\% des données d'entraînement ?
    \item \textbf{L'intégrité de l'évaluation} : Comment appliquer des techniques de sur-échantillonnage (SMOTE) sans provoquer de fuite de données (\textit{Data Leakage}) ?
    \item \textbf{La latence d'inférence} : Comment industrialiser le modèle pour soutenir un débit de dizaines de milliers de paquets par seconde sans devenir un goulot d'étranglement réseau ?
\end{enumerate}

\section{Objectifs du projet}

Les objectifs de ce projet de fin de module s'inscrivent dans une démarche MLOps :
\begin{enumerate}[leftmargin=2cm]
    \item \textbf{Prétraitement rigoureux} : Analyse ciblée des variables du dataset NSL-KDD et encodage adapté pour les modèles tabulaires.
    \item \textbf{Gestion avancée du déséquilibre} : Implémentation de pipelines \texttt{imbalanced-learn} stricts.
    \item \textbf{Modélisation orientée performance} : Optimisation d'algorithmes ensemblistes de pointe (Random Forest, XGBoost) capables de gérer des données creuses sans surapprentissage.
    \item \textbf{Industrialisation} : Développement d'un microservice asynchrone (FastAPI) optimisé en mémoire, couplé à une interface de monitoring et conteneurisé.
\end{enumerate}

\section{Environnement technique}

Le Tableau~\ref{tab:environnement} récapitule les technologies et versions utilisées dans ce projet.

\begin{table}[H]
    \centering
    \begin{tabular}{ll}
        \toprule
        \textbf{Technologie} & \textbf{Version} \\
        \midrule
        Python & 3.11 \\
        scikit-learn & $\geq$ 1.3.2 \\
        XGBoost & $\geq$ 2.0.3 \\
        imbalanced-learn & $\geq$ 0.11.0 \\
        pandas & $\geq$ 2.1.4 \\
        NumPy & $\geq$ 1.26.0 \\
        FastAPI & $\geq$ 0.100.0 \\
        Uvicorn (ASGI) & $\geq$ 0.23.0 \\
        Docker & 24.x \\
        Nginx & Alpine \\
        \bottomrule
    \end{tabular}
    \caption{Environnement technique du projet.}
    \label{tab:environnement}
\end{table}


% ═══════════════════════════════════════════
% CHAPITRE 2 — DONNÉES ET DÉFIS MÉTIERS
% ═══════════════════════════════════════════
\chapter{Données et Défis Métiers}

\section{Le Dataset NSL-KDD : Une référence épurée}

Le projet s'appuie sur le dataset \textbf{NSL-KDD} \cite{tavallaee2009}, qui corrige les défauts majeurs (redondances massives) de son prédécesseur KDD Cup 1999. Il comprend 125\,973 connexions pour l'entraînement et 22\,544 pour le test.

Chaque connexion est décrite par 41 attributs répartis en quatre familles, détaillées dans le Tableau~\ref{tab:features}.

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{lX}
        \toprule
        \textbf{Catégorie (nb)} & \textbf{Description et rôle dans la détection} \\
        \midrule
        Basiques (9) & En-têtes TCP/IP : durée, protocole (\texttt{tcp}/\texttt{udp}/\texttt{icmp}), service cible, flag de connexion, volume de données (\texttt{src\_bytes}, \texttt{dst\_bytes}). \\
        \addlinespace
        Contenu (13) & Charge utile (payload) : tentatives de login, accès root, création de fichiers. Essentielles pour détecter les attaques R2L et U2R. \\
        \addlinespace
        Temporelles (9) & Statistiques sur une fenêtre glissante de 2 secondes : taux d'erreurs SYN, taux de rejets. Critiques pour la détection DoS. \\
        \addlinespace
        Hôte (10) & Statistiques sur les 100 dernières connexions vers le même hôte : diversité des services, ratio de connexions identiques. \\
        \bottomrule
    \end{tabularx}
    \caption{Les 4 familles de caractéristiques du dataset NSL-KDD.}
    \label{tab:features}
\end{table}

L'intérêt métier de ce dataset réside dans sa diversité : au-delà des en-têtes TCP/IP basiques, il intègre des caractéristiques \textbf{temporelles} et calculées sur des \textbf{fenêtres d'hôtes}. Ce sont précisément ces métriques (ex: le taux de connexions rejetées vers un même hôte sur 2 secondes) qui permettent aux algorithmes de déduire un comportement de scan ou de déni de service.

\section{Taxonomie et déséquilibre pathologique}

Les 39 types d'attaques originaux ont été agrégés en 5 classes macroscopiques. Le Tableau~\ref{tab:desequilibre} illustre le défi principal du projet : le déséquilibre pathologique.

\begin{table}[H]
    \centering
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Classe} & \textbf{Type} & \textbf{Effectif} & \textbf{Proportion} & \textbf{Sévérité} \\
        \midrule
        Normal & Trafic légitime & 67\,343 & 53,46\% & N/A \\
        DoS & Déni de service & 45\,927 & 36,46\% & Élevée \\
        Probe & Reconnaissance & 11\,656 & 9,25\% & Modérée \\
        R2L & Accès distant & 995 & 0,79\% & Très Élevée \\
        U2R & Élévation de privilèges & 52 & 0,04\% & \textbf{Critique} \\
        \bottomrule
    \end{tabular}
    \caption{Distribution des classes dans le jeu d'entraînement et sévérité métier.}
    \label{tab:desequilibre}
\end{table}

Le ratio entre la classe la plus fréquente (Normal : 67\,343) et la plus rare (U2R : 52) est de \textbf{1\,295:1}. La classe la plus dangereuse (U2R, élévation de privilèges conduisant à un contrôle total) est la moins représentée. Un modèle d'apprentissage classique l'ignorera purement et simplement pour minimiser sa perte globale.

\section{Le défi de généralisation du jeu de test}

Contrairement à la majorité des projets académiques où le jeu de test est un simple sous-ensemble de la distribution d'entraînement, le jeu de test du NSL-KDD contient des \textbf{attaques inédites} (ex: \texttt{mailbomb}, \texttt{mscan}, \texttt{apache2}, \texttt{sqlattack}). Cela force les modèles à \textit{généraliser} des motifs comportementaux au lieu de mémoriser des signatures, rendant les scores de test plus réalistes mais aussi plus sévères.


% ═══════════════════════════════════════════
% CHAPITRE 3 — INGÉNIERIE ML ET ARCHITECTURE
% ═══════════════════════════════════════════
\chapter{Ingénierie ML et Justification des Choix}

Ce chapitre détaille les décisions architecturales et techniques prises pour construire un pipeline robuste, en s'éloignant des choix "par défaut".

\section{Prétraitement et dimensionnalité}

La préparation des données a nécessité deux transformations critiques :
\begin{enumerate}
    \item \textbf{Encodage One-Hot des variables textuelles} : Les attributs \texttt{protocol\_type} (3 valeurs), \texttt{service} (70 valeurs) et \texttt{flag} (11 valeurs) ne possèdent pas de relation ordinale. Leur encodage a généré un espace d'entrée de \textbf{122 variables} très creuses (sparse). Pour gérer l'asymétrie avec le jeu de test (qui contient des services non vus à l'entraînement), la méthode \texttt{reindex()} de pandas a été employée pour garantir une stricte correspondance des dimensions, en remplissant les colonnes manquantes par des zéros.
    \item \textbf{Standardisation rigoureuse} : Un \texttt{StandardScaler} a été ajusté \textit{uniquement} sur le jeu d'entraînement (\texttt{fit\_transform}), puis appliqué au jeu de test via \texttt{transform()} sans recalcul des paramètres. L'objet scaler a été sérialisé (\texttt{scaler.pkl}) pour l'inférence en production.
\end{enumerate}

\section{Prévention du Data Leakage avec SMOTE}

Pour traiter le déséquilibre extrême des attaques R2L et U2R, l'algorithme d'interpolation \textbf{SMOTE} \cite{chawla2002} a été retenu. SMOTE génère des points synthétiques en interpolant entre des instances existantes de la classe minoritaire via l'algorithme des K-plus-proches voisins, enrichissant ainsi l'espace décisionnel sans simple duplication.

Cependant, son implémentation est souvent source d'erreurs fatales en ML : appliquer SMOTE sur l'ensemble du dataset avant l'entraînement contamine la validation avec des données synthétiques.

\textbf{Choix architectural} : Nous avons intégré SMOTE à l'intérieur d'un objet \texttt{ImbPipeline} (librairie \texttt{imbalanced-learn} \cite{lemaitre2017}). Ainsi, lors de l'optimisation des hyperparamètres, la génération de données synthétiques s'effectue dynamiquement \textit{uniquement} sur les plis (folds) d'entraînement, préservant la pureté absolue du pli de validation.

\begin{lstlisting}[caption={Pipeline SMOTE intégré à la validation croisée.}]
from imblearn.pipeline import Pipeline as ImbPipeline
from imblearn.over_sampling import SMOTE

xgb_pipeline = ImbPipeline([
    ('smote', SMOTE(random_state=42)),
    ('classifier', XGBClassifier(
        objective='multi:softprob', num_class=5,
        eval_metric='mlogloss', random_state=42
    ))
])
\end{lstlisting}

Le compromis technique assumé ici est le \textbf{coût computationnel} : la génération dynamique de milliers de points à chaque itération allonge considérablement le temps d'entraînement.

\section{Justification du choix des algorithmes}

Plutôt que d'employer des algorithmes de Deep Learning (réseaux de neurones denses), très coûteux à mettre en production pour des données tabulaires, nous avons ciblé les modèles ensemblistes basés sur les arbres : \textbf{Random Forest} (Bagging) et \textbf{XGBoost} \cite{chen2016} (Gradient Boosting).

Ces choix sont motivés par plusieurs facteurs d'ingénierie :
\begin{itemize}
    \item \textbf{Tolérance à la sparsité} : XGBoost possède des optimisations internes (Sparsity-aware Split Finding) excellentes pour gérer nos 122 variables encodées.
    \item \textbf{Latence d'inférence} : Les arbres de décision compilés permettent des temps de prédiction de l'ordre de la microseconde par connexion, un prérequis pour un IDS temps réel.
    \item \textbf{Résistance aux outliers} : Contrairement à la régression logistique, les arbres ne dépendent pas des distances euclidiennes et gèrent nativement les valeurs extrêmes communes dans le trafic réseau (\texttt{src\_bytes} pouvant varier de 0 à plusieurs millions).
    \item \textbf{Interprétabilité} : Les deux modèles fournissent nativement un classement d'importance des caractéristiques, permettant d'identifier les variables réseau les plus discriminantes.
\end{itemize}

\section{Stratégie d'optimisation (RandomizedSearchCV)}

L'optimisation des hyperparamètres a été menée via une \texttt{RandomizedSearchCV} ($n\_iter = 5$, limité par les contraintes de temps de calcul) couplée à une validation croisée stratifiée (\texttt{StratifiedKFold}, $K=3$). La métrique cible choisie est le \textbf{F1-Score Macro}. Contrairement à l'exactitude globale (qui aurait favorisé la classe "Normal"), le F1-Score Macro force l'algorithme à accorder une importance égale à la classe U2R (52 exemples) et à la classe Normal (67\,343 exemples).

Le Tableau~\ref{tab:hyperparams} présente les espaces de recherche définis.

\begin{table}[H]
    \centering
    \begin{tabular}{lll}
        \toprule
        \textbf{Algorithme} & \textbf{Hyperparamètre} & \textbf{Valeurs explorées} \\
        \midrule
        \multirow{4}{*}{Random Forest} & \texttt{n\_estimators} & \{100, 200, 300\} \\
         & \texttt{max\_depth} & \{15, 25, 35, None\} \\
         & \texttt{min\_samples\_split} & \{2, 5, 10\} \\
         & \texttt{min\_samples\_leaf} & \{1, 2, 4\} \\
        \midrule
        \multirow{5}{*}{XGBoost} & \texttt{n\_estimators} & \{100, 200, 300\} \\
         & \texttt{max\_depth} & \{6, 10, 15\} \\
         & \texttt{learning\_rate} & \{0,01 ; 0,1 ; 0,2\} \\
         & \texttt{subsample} & \{0,8 ; 1,0\} \\
         & \texttt{colsample\_bytree} & \{0,8 ; 1,0\} \\
        \bottomrule
    \end{tabular}
    \caption{Espaces de recherche des hyperparamètres.}
    \label{tab:hyperparams}
\end{table}


% ═══════════════════════════════════════════
% CHAPITRE 4 — RÉSULTATS
% ═══════════════════════════════════════════
\chapter{Résultats Expérimentaux et Analyse}

\section{Performances globales}

Le Tableau~\ref{tab:resultats-globaux} synthétise les scores sur le jeu de test NSL-KDD.

\begin{table}[H]
    \centering
    \begin{tabular}{lccc}
        \toprule
        \textbf{Métrique} & \textbf{Random Forest + SMOTE} & \textbf{XGBoost + SMOTE} & \textbf{Différence} \\
        \midrule
        Exactitude globale & 75,00\% & \textbf{78,95\%} & +3,95\% \\
        F1-Score Macro & 0,5342 & \textbf{0,6314} & +0,0972 \\
        F1-Score Pondéré & 0,7087 & \textbf{0,7582} & +0,0495 \\
        \bottomrule
    \end{tabular}
    \caption{Comparaison des scores globaux sur le jeu de test NSL-KDD.}
    \label{tab:resultats-globaux}
\end{table}

L'architecture XGBoost s'impose nettement, validant l'intérêt du \textit{Gradient Boosting} face au \textit{Bagging} pour corriger itérativement les erreurs sur les classes dures.

\section{Analyse granulaire des classes critiques}

La véritable valeur du pipeline réside dans sa capacité à identifier les attaques complexes. Le Tableau~\ref{tab:resultats-classe} détaille les performances.

\begin{table}[H]
    \centering
    \begin{tabular}{l|ccc|ccc}
        \toprule
        & \multicolumn{3}{c|}{\textbf{Random Forest + SMOTE}} & \multicolumn{3}{c}{\textbf{XGBoost + SMOTE}} \\
        \textbf{Classe} & Précision & Rappel & F1 & Précision & Rappel & F1 \\
        \midrule
        Normal & 0,6675 & 0,9667 & 0,7895 & 0,7236 & 0,9448 & 0,8196 \\
        DoS & 0,9543 & 0,6990 & 0,8071 & 0,9657 & 0,7704 & 0,8572 \\
        Probe & 0,7707 & 0,5753 & 0,6589 & 0,7449 & 0,7124 & 0,7283 \\
        R2L & 0,8333 & 0,2153 & 0,3423 & \textbf{0,9711} & 0,3153 & 0,4759 \\
        U2R & 0,1818 & 0,1463 & 0,1621 & \textbf{0,7188} & 0,3415 & \textbf{0,4630} \\
        \bottomrule
    \end{tabular}
    \caption{Performances détaillées par classe (Précision, Rappel, F1-Score).}
    \label{tab:resultats-classe}
\end{table}

\textbf{L'impact majeur de SMOTE + XGBoost} s'observe sur les attaques U2R : le F1-Score a quasiment triplé (0,16 vers 0,46), avec une précision étonnante de 71,88\%. Sur la classe R2L, XGBoost atteint une précision de 97,11\% (lorsque le modèle lève une alerte R2L, il a presque toujours raison). Le rappel (Recall) modeste de ces classes s'explique par les attaques inédites présentes dans le jeu de test.

\section{Comparaison visuelle des performances par classe}

La Figure~\ref{fig:comparison} offre une comparaison directe du F1-Score, de la Précision et du Rappel entre Random Forest et XGBoost pour chacune des 5 classes.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth, height=9cm, keepaspectratio]{../reports/model_comparison.png}
    \caption{Comparaison RF vs XGBoost par classe : F1-Score, Précision et Rappel.}
    \label{fig:comparison}
\end{figure}

On observe que XGBoost surpasse systématiquement Random Forest sur toutes les classes, avec un avantage particulièrement marqué sur les classes critiques R2L et U2R.

\section{Matrices de confusion et analyse d'erreurs}

L'analyse des matrices de confusion (Figure~\ref{fig:confusion}) met en lumière la nature des erreurs de classification.

\begin{figure}[p]
    \centering
    \includegraphics[width=\textwidth]{../reports/confusion_matrices.png}
    \caption{Matrices de confusion de Random Forest (gauche) et XGBoost (droite) sur le jeu de test NSL-KDD. Les valeurs en diagonale représentent les prédictions correctes.}
    \label{fig:confusion}
\end{figure}

L'analyse de la matrice d'XGBoost révèle les principaux motifs d'erreur :
\begin{itemize}
    \item Les attaques \textbf{R2L} (exploitation de failles FTP, tentatives d'accès par mot de passe) sont majoritairement confondues avec du trafic "Normal". D'un point de vue réseau, ces connexions ressemblent trait pour trait à une connexion légitime.
    \item Les attaques \textbf{Probe} sont parfois classifiées comme "Normal", ce qui s'explique par le fait que certaines reconnaissances réseau sont très discrètes (scan lent).
    \item Le modèle confond rarement les classes entre elles (faibles valeurs hors diagonale), signe d'une bonne discrimination.
\end{itemize}

\section{Courbes ROC et capacité de discrimination}

Les courbes ROC (\textit{Receiver Operating Characteristic}) évaluent la capacité de discrimination binaire du modèle pour chaque classe (approche One-vs-Rest). La Figure~\ref{fig:roc} présente les résultats.

\begin{figure}[p]
    \centering
    \includegraphics[width=\textwidth]{../reports/roc_curves.png}
    \caption{Courbes ROC (One-vs-Rest) avec aire sous la courbe (AUC) par classe pour Random Forest (gauche) et XGBoost (droite).}
    \label{fig:roc}
\end{figure}

Les valeurs d'AUC confirment la hiérarchie des performances : les classes DoS et Normal atteignent des AUC proches de 0,99 (discrimination quasi parfaite), tandis que les classes R2L et U2R présentent des AUC plus modestes mais significativement supérieures au seuil aléatoire de 0,5.

\section{Importance des caractéristiques}

La Figure~\ref{fig:importance} présente les "Top 20 Features" les plus discriminantes retenues par chaque modèle.

\begin{figure}[p]
    \centering
    \includegraphics[width=\textwidth]{../reports/feature_importance.png}
    \caption{Top 20 des caractéristiques les plus importantes pour Random Forest (gauche) et XGBoost (droite).}
    \label{fig:importance}
\end{figure}

Il est intéressant de noter que le modèle s'appuie massivement sur des caractéristiques \textbf{dérivées} (taux d'erreurs SYN, ratio de services) plutôt que sur des ports bruts. Notamment :
\begin{itemize}
    \item \texttt{dst\_host\_srv\_serror\_rate} et \texttt{serror\_rate} : indicateurs forts d'attaques DoS de type \textit{SYN flood}.
    \item \texttt{service\_http} et \texttt{service\_private} : discriminants pour les attaques R2L.
    \item \texttt{logged\_in} : indicateur de session authentifiée, pertinent pour distinguer les intrusions des connexions légitimes.
\end{itemize}
C'est ce qui confère au modèle sa capacité de détection \textbf{comportementale} et sa robustesse face au polymorphisme des attaques.

\section{Benchmark de latence d'inférence}

Dans la perspective d'un déploiement en temps réel, nous avons mesuré la latence d'inférence brute du modèle XGBoost final sur des batchs de tailles croissantes (Tableau~\ref{tab:benchmark}).

\begin{table}[H]
    \centering
    \begin{tabular}{lcc}
        \toprule
        \textbf{Taille du batch} & \textbf{Latence mesurée} & \textbf{Débit estimé} \\
        \midrule
        1 connexion & $\sim 7$ ms & $\sim 143$ conn/s \\
        1\,000 connexions & $\sim 28$ ms & $\sim 35\,714$ conn/s \\
        10\,000 connexions & $\sim 218$ ms & $\sim 45\,872$ conn/s \\
        \bottomrule
    \end{tabular}
    \caption{Benchmark de latence d'inférence du modèle XGBoost (inférence pure, hors prétraitement).}
    \label{tab:benchmark}
\end{table}

\textbf{Note importante} : ces mesures ne concernent que le temps d'inférence pure (\texttt{model.predict()}). En production, le temps de prétraitement (One-Hot Encoding + Scaling) ajouterait une surcharge supplémentaire. Le débit réel serait donc légèrement inférieur aux valeurs théoriques présentées ici.


% ═══════════════════════════════════════════
% CHAPITRE 5 — DÉPLOIEMENT MLOps
% ═══════════════════════════════════════════
\chapter{Industrialisation (MLOps) et Déploiement}

La valeur d'un modèle d'apprentissage réside dans sa capacité à être interrogé en production. Ce chapitre décrit l'architecture d'industrialisation mise en place.

\section{Architecture globale du système}

Le système suit le paradigme des \textbf{microservices}, séparant le backend d'inférence du frontend de visualisation. La Figure~\ref{fig:architecture} illustre les interactions entre les composants.

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.92\textwidth}{
        \centering
        \vspace{0.5cm}
        \textbf{Architecture du Système IDS}\\[0.8cm]
        \begin{tabular}{ccc}
            \fbox{\parbox{3.5cm}{\centering\textbf{Dashboard}\\HTML/CSS/JS\\Port 8080 (Nginx)}} &
            $\xrightarrow[\text{JSON}]{\text{HTTP REST}}$ &
            \fbox{\parbox{3.5cm}{\centering\textbf{API FastAPI}\\Python Async\\Port 8000 (Uvicorn)}} \\
            & & $\downarrow$ \\
            & & \fbox{\parbox{3.5cm}{\centering\textbf{Artefacts ML}\\XGBoost (.pkl)\\Scaler (.pkl)\\Colonnes (.pkl)}} \\
        \end{tabular}\\[0.5cm]
        \rule{0.85\textwidth}{0.3pt}\\[0.3cm]
        \textit{L'ensemble est orchestré dans des conteneurs Docker via docker-compose.}
        \vspace{0.3cm}
    }}
    \caption{Architecture microservices du système IDS.}
    \label{fig:architecture}
\end{figure}

\section{API Asynchrone : FastAPI}

Le modèle entraîné (sérialisé via \texttt{joblib}) est exposé via une API REST développée avec \textbf{FastAPI}. Une attention particulière a été portée à l'optimisation mémoire :
\begin{lstlisting}[caption={Gestion du cycle de vie : chargement unique au démarrage.}]
@asynccontextmanager
async def lifespan(app: FastAPI):
    global model, scaler, model_columns
    model = joblib.load('models/best_model.pkl')
    scaler = joblib.load('models/scaler.pkl')
    model_columns = joblib.load('models/model_columns.pkl')
    # Pre-transform du jeu de test en memoire
    yield
    # Nettoyage a l'arret du serveur
\end{lstlisting}
Le modèle lourd n'est monté en RAM qu'une seule fois au démarrage du serveur (\textit{Cold Start}), permettant ensuite de traiter les requêtes HTTP d'inférence en quelques millisecondes.

L'API expose trois endpoints principaux :

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{llX}
        \toprule
        \textbf{Méthode} & \textbf{Route} & \textbf{Description} \\
        \midrule
        \texttt{GET} & \texttt{/} & Vérification de santé (\textit{health check}). \\
        \texttt{POST} & \texttt{/predict} & Inférence sur une connexion personnalisée (JSON). \\
        \texttt{GET} & \texttt{/predict/random} & Simulation de flux : sélection aléatoire d'une connexion du jeu de test, prédiction et comparaison avec le vrai label. \\
        \bottomrule
    \end{tabularx}
    \caption{Endpoints de l'API REST.}
    \label{tab:endpoints}
\end{table}

\section{Tableau de bord de type SOC}

Pour visualiser les détections, un tableau de bord a été développé en HTML/CSS/JavaScript vanilla, simulant l'interface d'un centre opérationnel de sécurité (SOC). Il interroge l'API à haute fréquence (simulation d'un flux continu de paquets), et propose :
\begin{itemize}
    \item Des compteurs animés en temps réel : paquets analysés, menaces détectées, taux de précision, latence moyenne.
    \item Une jauge dynamique de niveau de menace basée sur le ratio d'attaques récentes.
    \item Un terminal de logs affichant chaque connexion analysée avec sa prédiction, son niveau de confiance et son statut (correct/erreur).
    \item Un mode clair/sombre avec persistance du choix via \texttt{localStorage}.
\end{itemize}

\section{Conteneurisation via Docker}

L'intégralité du pipeline applicatif est orchestrée avec \textbf{Docker}. Le fichier \texttt{docker-compose.yml} sépare l'environnement en deux microservices :
\begin{enumerate}
    \item \textbf{api} : Conteneur Python (image \texttt{python:3.11-slim}) exécutant le serveur FastAPI/Uvicorn sur le port 8000, avec un \textit{healthcheck} automatique.
    \item \textbf{dashboard} : Conteneur Nginx Alpine servant le fichier HTML statique sur le port 8080.
\end{enumerate}

Le déploiement sur n'importe quel serveur s'effectue via :
\begin{lstlisting}[language=bash, numbers=none]
cd docker && docker compose up -d --build
\end{lstlisting}
Cette conteneurisation garantit une reproductibilité stricte, indépendante du système hôte, et supprime le problème classique du "ça marche sur ma machine".


% ═══════════════════════════════════════════
% CHAPITRE 6 — CONCLUSION ET LIMITES
% ═══════════════════════════════════════════
\chapter{Conclusion et Perspectives}

\section{Bilan du projet}

Ce projet a couvert l'ensemble du cycle de vie d'une solution de Machine Learning pour la cybersécurité :
\begin{enumerate}
    \item \textbf{Prétraitement} : Encodage One-Hot, standardisation rigoureuse avec sérialisation du scaler, gestion des asymétries entre train et test.
    \item \textbf{Gestion du déséquilibre} : Pipeline SMOTE intégré à la validation croisée, empêchant toute fuite de données.
    \item \textbf{Modélisation} : Comparaison Random Forest vs XGBoost avec optimisation par \texttt{RandomizedSearchCV} et validation croisée stratifiée.
    \item \textbf{Résultats} : XGBoost atteint 78,95\% d'exactitude et un F1 Macro de 0,631. Le résultat le plus significatif est le triplement du F1-Score de la classe U2R (0,16 $\rightarrow$ 0,46) et la précision de 97,11\% sur R2L.
    \item \textbf{Déploiement} : API FastAPI asynchrone, dashboard interactif et conteneurisation Docker, avec un débit théorique de 45\,000+ connexions/seconde.
\end{enumerate}

Face aux écueils classiques du dataset NSL-KDD, le modèle a démontré qu'il n'avait pas simplement mémorisé la classe majoritaire, mais qu'il avait extrait une véritable sémantique de l'intrusion.

\section{Limites structurelles et axes d'amélioration}

En tant que projet d'ingénierie, il est essentiel de souligner de manière critique les limites actuelles du système :

\begin{enumerate}
    \item \textbf{Le biais temporel (Look-ahead Bias)} : Bien qu'académiquement standard sur NSL-KDD, l'utilisation d'un \texttt{StratifiedKFold} pour la validation croisée d'un trafic réseau implique d'utiliser des données du "futur" pour prédire des données du "passé". En production, une division strictement chronologique (\textit{TimeSeriesSplit}) serait impérative.

    \item \textbf{Absence de "Cost-Sensitive Learning"} : En cybersécurité, un Faux Négatif (intrusion non détectée) coûte infiniment plus cher qu'un Faux Positif (alerte injustifiée). Le seuil de décision par défaut de XGBoost (0.5) devrait idéalement être abaissé pour privilégier le Rappel des attaques critiques, au détriment de l'Exactitude globale.

    \item \textbf{Explicabilité (XAI)} : Pour qu'un opérateur réseau fasse confiance à un modèle, la prédiction doit être explicable. L'intégration d'outils comme \textbf{SHAP} \cite{lundberg2017} (SHapley Additive exPlanations) pour indiquer \textit{pourquoi} la connexion a été bloquée est aujourd'hui un standard indispensable dans un produit MLOps industriel.

    \item \textbf{Surveillance du modèle (Data Drift)} : Les vecteurs d'attaques évoluent quotidiennement. L'architecture actuelle ne prévoit pas de boucle de ré-entraînement automatisée pour pallier la dérive des concepts (\textit{Concept Drift}), ce qui entraînerait une dégradation inévitable des performances sur le long terme.

    \item \textbf{Dataset daté} : Le NSL-KDD, bien que référence académique, date de 2009 et ne reflète pas les attaques modernes (ransomwares, attaques par supply chain). Des datasets plus récents comme CICIDS2017 ou CSE-CIC-IDS2018 seraient à considérer pour des travaux futurs.
\end{enumerate}


% ═══════════════════════════════════════════
% BIBLIOGRAPHIE
% ═══════════════════════════════════════════
\begin{thebibliography}{10}
\addcontentsline{toc}{chapter}{Références Bibliographiques}

\bibitem{tavallaee2009}
M. Tavallaee, E. Bagheri, W. Lu et A. A. Ghorbani,
\textit{A Detailed Analysis of the KDD CUP 99 Data Set},
IEEE Symposium on Computational Intelligence for Security and Defense Applications, 2009.

\bibitem{chawla2002}
N. V. Chawla, K. W. Bowyer, L. O. Hall et W. P. Kegelmeyer,
\textit{SMOTE: Synthetic Minority Over-sampling Technique},
Journal of Artificial Intelligence Research, vol. 16, pp. 321--357, 2002.

\bibitem{chen2016}
T. Chen et C. Guestrin,
\textit{XGBoost: A Scalable Tree Boosting System},
Proceedings of the 22nd ACM SIGKDD International Conference on Knowledge Discovery and Data Mining, pp. 785--794, 2016.

\bibitem{breiman2001}
L. Breiman,
\textit{Random Forests},
Machine Learning, vol. 45, no. 1, pp. 5--32, 2001.

\bibitem{lemaitre2017}
G. Lemaître, F. Nogueira et C. K. Aridas,
\textit{Imbalanced-learn: A Python Toolbox to Tackle the Curse of Imbalanced Datasets in Machine Learning},
Journal of Machine Learning Research, vol. 18, no. 17, pp. 1--5, 2017.

\bibitem{lundberg2017}
S. M. Lundberg et S.-I. Lee,
\textit{A Unified Approach to Interpreting Model Predictions},
Advances in Neural Information Processing Systems (NeurIPS), vol. 30, 2017.

\bibitem{sklearn}
F. Pedregosa et al.,
\textit{Scikit-learn: Machine Learning in Python},
Journal of Machine Learning Research, vol. 12, pp. 2825--2830, 2011.

\bibitem{buczak2016}
A. L. Buczak et E. Guven,
\textit{A Survey of Data Mining and Machine Learning Methods for Cyber Security Intrusion Detection},
IEEE Communications Surveys \& Tutorials, vol. 18, no. 2, pp. 1153--1176, 2016.

\end{thebibliography}

\end{document}
